// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyDtljqh5uXLU yGggffKyjBT5qVdCzL2e4s",
    authDomain: "ecomm-95afe.firebaseapp.com",
    databaseURL: "https://ecomm-95afe.firebaseio.com",
    projectId: "ecomm-95afe",
    storageBucket: "ecomm-95afe.appspot.com",
    messagingSenderId: "367596664431",
    appId: "1:367596664431:web:3203021ae092a5acf0bf9a",
    measurementId: "G-7YK9YHLR44"
  }
  // Initialize Firebase
  //firebase.initializeApp(firebaseConfig);
  //firebase.analytics();
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
